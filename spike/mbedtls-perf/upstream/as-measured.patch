diff --git a/tf-psa-crypto/drivers/builtin/src/bignum_core.c b/tf-psa-crypto/drivers/builtin/src/bignum_core.c
index fc1b537..aa6cf90 100644
--- a/tf-psa-crypto/drivers/builtin/src/bignum_core.c
+++ b/tf-psa-crypto/drivers/builtin/src/bignum_core.c
@@ -452,10 +452,12 @@ mbedtls_mpi_uint mbedtls_mpi_core_sub(mbedtls_mpi_uint *X,
     mbedtls_mpi_uint c = 0;
 
     for (size_t i = 0; i < limbs; i++) {
-        mbedtls_mpi_uint z = mbedtls_ct_mpi_uint_if(mbedtls_ct_uint_lt(A[i], c),
-                                                    1, 0);
+        /* SPIKE-F: _if_else_0() is (condition & if1), no barrier needed;
+         * _if(cond, 1, 0) has to build ~condition via compiler_opaque(). */
+        mbedtls_mpi_uint z = mbedtls_ct_mpi_uint_if_else_0(
+            mbedtls_ct_uint_lt(A[i], c), 1);
         mbedtls_mpi_uint t = A[i] - c;
-        c = mbedtls_ct_mpi_uint_if(mbedtls_ct_uint_lt(t, B[i]), 1, 0) + z;
+        c = mbedtls_ct_mpi_uint_if_else_0(mbedtls_ct_uint_lt(t, B[i]), 1) + z;
         X[i] = t - B[i];
     }
 
@@ -493,7 +495,7 @@ mbedtls_mpi_uint mbedtls_mpi_core_mla(mbedtls_mpi_uint *d, size_t d_len,
 
     while (excess_len--) {
         *d += c;
-        c = mbedtls_ct_mpi_uint_if(mbedtls_ct_uint_lt(*d, c), 1, 0);
+        c = mbedtls_ct_mpi_uint_if_else_0(mbedtls_ct_uint_lt(*d, c), 1); /* SPIKE-F */
         d++;
     }
 
diff --git a/tf-psa-crypto/utilities/constant_time_impl.h b/tf-psa-crypto/utilities/constant_time_impl.h
index 334764e..f079c15 100644
--- a/tf-psa-crypto/utilities/constant_time_impl.h
+++ b/tf-psa-crypto/utilities/constant_time_impl.h
@@ -264,6 +264,9 @@ static inline mbedtls_ct_uint_t mbedtls_ct_if(mbedtls_ct_condition_t condition,
 #endif
 }
 
+/* SPIKE-E: at -Os on targets with no asm path, GCC leaves this out of line, so
+ * every use costs a call plus (on Xtensa) a register-window entry and spills. */
+__attribute__((always_inline))
 static inline mbedtls_ct_condition_t mbedtls_ct_uint_lt(mbedtls_ct_uint_t x, mbedtls_ct_uint_t y)
 {
 #if defined(MBEDTLS_CT_AARCH64_ASM) && (defined(MBEDTLS_CT_SIZE_32) || defined(MBEDTLS_CT_SIZE_64))
@@ -343,6 +346,16 @@ static inline mbedtls_ct_condition_t mbedtls_ct_uint_lt(mbedtls_ct_uint_t x, mbe
                   :
                   );
     return (mbedtls_ct_condition_t) x;
+#elif 1 /* SPIKE-F: barrier-free branchless generic implementation */
+    /* The same six-operation sequence the Arm/AArch64/x86 asm paths above
+     * implement (eor / sub / bic / and / orr / asr), written in C. It contains
+     * no comparison and no branch, so there is nothing for the compiler to
+     * lower into a conditional jump, and no compiler_opaque() barrier is
+     * needed. The previous generic code used six of them. */
+    const mbedtls_ct_uint_t s = x ^ y;              /* eor */
+    const mbedtls_ct_uint_t d = x - y;              /* sub */
+    const mbedtls_ct_uint_t r = (d & ~s) | (y & s); /* bic / and / orr */
+    return (mbedtls_ct_condition_t) (0u - (r >> (MBEDTLS_CT_SIZE - 1)));
 #else
     /* Ensure that the compiler cannot optimise the following operations over x and y,
      * even if it knows the value of x and y.
